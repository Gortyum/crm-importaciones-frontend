# URL base del backend FastAPI.
# - Dev local: http://localhost:8000
# - Produccion (confirmado / Railway): https://crm-importaciones-backend-production.up.railway.app
# Requiere el prefijo VITE_ para que Vite la exponga al bundle.
# Si se deja vacia, el frontend usa rutas relativas (/api, /uploads)
# y confia en el proxy de Vite para desarrollo.
VITE_API_URL=http://localhost:8000
